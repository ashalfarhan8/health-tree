import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#5762D5",
    secondary: "#6E7DAB",
    ternary: "#575366",
    accent: "#32292F",
  },
  fonts: {
    heading: "Mulish",
    body: "Mulish",
  },
});

export default theme;
